let tableau = [3, 5, 8, 10];
if (tableau.length === 0) {
    
    console.log("tableau vide");

} else {
    let somme = tableau.reduce((acc, val) => acc + val, 0);
    
    console.log("moyenne ", somme / tableau.length);
}
